import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SnapCoat CRM - CRM Built for Coating Contractors',
  description: 'Streamline your workflow, manage customer relationships, track projects, and grow your business faster with the all-in-one CRM solution designed specifically for the coating industry.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}

